#### 函数：rand
语法：rand(<随机范围>)
返回：根据<随机范围>生成一个随机数。
参数**<随机范围>**：当<随机范围>为整数时，生成0至<随机范围>-1之间的随机整数；<随机范围>缺省时生成0至1之间的随机小数。非必要参数；数值类型；参数名已省略。
>例子：生成0至100之间的随机整数。
NLC片段：rand(101)
>例子：生成0至1之间的随机小数。
NLC片段：rand()