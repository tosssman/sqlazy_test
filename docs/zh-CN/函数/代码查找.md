#### 函数：代码查找
语法：<对象参数> 代码查找(<code值>)
返回：数据。在表<对象参数>的第一个字段中用<code值>进行查找，匹配到记录后，返回第2个字段的值。匹配不上则返回空。
参数**<对象参数>**：key-value或code-value结构的代码表。
例子：员工表有若干字段，前2个是员工ID和员工姓名。现在用1001对员工表进行代码查找。
NLC片段：员工表 代码查找(1001)		//返回某个员工姓名。