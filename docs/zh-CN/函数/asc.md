#### 函数：asc
语法：asc(<字符>)
返回：如果<字符>是8位字符，比如英文及其扩展字符，则返回ascii码；如果<字符>是16位字符比如中日韩亚洲字符，则返回unicode值。
参数**<字符>** 字符。必要参数；字符类型；参数名已省略。
>例子：计算"d"的ascii码
NLC片段：asc("d")				//结果是100
>例子：计算"中"的unicode值。
NLC片段：asc("中")				//结果20013